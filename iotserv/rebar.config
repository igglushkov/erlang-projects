{erl_opts, [debug_info]}.
{deps, [{jsx, "3.1.0"}]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [iotserv]}
]}.

{dialyzer, [
    {plt_apps, all_deps},
    {warnings, [unmatched_returns, error_handling, no_return]},
    {plt_location, local}
]}.
